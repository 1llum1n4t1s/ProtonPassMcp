# システム設計

## 目的と境界

認証済み Proton Pass CLI セッションを使い、ローカルの MCP クライアントへ読み取り専用の検索・取得機能を提供する。接続は stdio のみで、認証や Proton 側への通信は外部の `pass-cli` が担う。サーバー自身はログイン、ログアウト、保管庫の更新、トークン保存を行わない。利用手順とツール一覧は [README.md](README.md)、変更・検証規約は [AGENTS.md](AGENTS.md) を参照する。

## コンポーネントとデータフロー

| 対象 | 責務 |
|---|---|
| `src/server.mjs` | 環境変数からクライアントを生成し、manifest のバージョンで MCP サーバーを登録する。Zod の strict スキーマで入力を制限し、6ツールの処理を直列化する。成功時は JSON テキストと `structuredContent`、失敗時は安全なエラー文を返す。 |
| `src/pass.mjs` / `PassClient` | セッション環境、認証確認、CLI 引数、JSON 解釈、メタデータ抽出、検索とページングを管理する。 |
| `src/pass.mjs` / `runCli` | シェルを介さず子プロセスを実行し、キャンセル・時間・出力サイズを制限する。生エラーを公開用メッセージへ分類する。 |
| `test/pass.test.mjs` | ダミー runner と子プロセスを使い、認証失敗、秘密の抑制、検索続行、単一フィールド取得、直列実行を検証する。 |
| `scripts/verify.mjs` | 実セッションで stdio 接続、ツール登録、セッション・保管庫・共有一覧、理由の入力検証を確認する。 |

MCP 入力 → スキーマ検証 → `exclusive` キュー → `PassClient` → `pass-cli info` → 読み取り CLI 操作 → 結果の整形 → MCP 応答の順に処理する。一覧とノート照合は `--output json` を指定して JSON を解釈する。`read_field` の出力処理は後述する。`session_status` は `info` の成功を `authenticated: true` に変換し、生出力を返さない。

## 検索とページング

- アイテム一覧は ID 順へ整列し、タイトルを大文字小文字を区別せず部分一致で絞り込む。`offset` / `limit` で返し、続きは `next_offset` で示す。各呼び出しで一覧を再取得する。
- ノート検索は保管庫ごとにノート一覧を取得し、各ノートのタイトルと本文を大文字小文字を区別して部分一致で調べる。本文は照合にのみ使い、結果は一覧由来のメタデータに限定する。
- ノート検索は1回最大3件。各件の処理後、開始から15秒以上経過していればそのページを終える。これは呼び出し全体の厳密な15秒タイムアウトではない。
- カーソルは SHA-256 の fingerprint と処理済み offset を base64url 化する。fingerprint は保管庫、状態条件、検索語、整列した ID と状態を対象にし、条件や対象一覧の変更時は再検索を要求する。本文・タイトルの更新は fingerprint に含まれず、検索はスナップショットを保証しない。

## 不変条件と設計上の選択

- 各データ取得の直前に `info` を実行する。認証失敗時はデータ取得へ進まない。追加のプロセス実行コストと引き換えに、既存セッションの確認を各操作へ適用する。
- ツール全体をキューで直列化し、失敗後も次の要求を実行する。同一セッションへの並行呼び出しを避けるため、長い検索中は後続要求が待機する。
- `execFile` は `shell: false`、出力上限8 MiB、`info` は5秒、それ以外は20秒のタイムアウトを使う。MCP のキャンセル信号を子プロセスまで渡す。
- 子プロセスへ PAT と既存の `PROTON_PASS_AGENT_REASON` を継承しない。検索とフィールド取得では入力の理由を対象操作へ渡す。理由はサーバー入力で前後空白除去後5〜1000文字を要求するが、対話的な承認機構ではない。
- 一覧・検索は許可したフィールドだけを返す。`read_field` は `item view --field` の標準出力末尾の改行を1つ除き、文字列の `value` として返す。この経路は `--output` を指定せず、JSON 解釈や値の形式変換も行わないため、返却文字列の形式は CLI の出力に依存する。秘密を結果へ出す境界はこのツールにある。
- CLI の生 stderr と未知の内部エラーを MCP 応答へ露出させない。サーバー独自の本文・生エラーログ保存は実装していないが、受信した値の扱いは接続先クライアントに委ねられる。
